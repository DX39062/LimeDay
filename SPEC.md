# 青柠日记产品与技术规格说明书

- 版本：2.0 Draft
- 状态：Android 重构基线
- 当前目标平台：Android 8.0 及以上（API 26+）
- 未来目标平台：Windows、macOS；多端同步

## 1. 文档目标

本规格用于指导青柠日记在当前仓库内进行新一轮开发。当前版本以 Android 客户端为唯一交付目标，使用 Flutter 重构现有应用，并为后续 Windows、macOS、多端同步、OneDrive 和 WebDAV 支持预留稳定的数据与模块边界。

当前阶段不实现桌面客户端和云同步，但不得采用明显阻碍后续多平台开发的数据结构或平台耦合方案。

## 2. 产品定位

青柠日记是一款离线优先、轻量、现代的每日计划与复盘应用。用户可以围绕某一天安排待办、记录完成情况、填写结构化复盘，并按需调用自己的大语言模型服务生成当日总结。

产品体验应具备以下特征：

- 打开即可记录，无强制注册和登录。
- 本地数据是唯一可信源，无网络时全部核心功能可用。
- 信息层级清楚，操作直接，避免复杂配置和多余页面。
- 界面具有现代感，但不依赖夸张装饰、过度动画或大面积营销式布局。
- 用户始终清楚内容保存在何处、何时会发送到第三方服务。

## 3. 当前版本目标

### 3.1 产品目标

- 使用 Flutter 完成 Android 客户端重构。
- 保留并完善每日待办、每日复盘和智能总结能力。
- 提供适合手机和平板的自适应布局。
- 保留现有 Android 安装用户的数据和模型配置。
- 建立可供未来桌面端复用的领域模型、数据访问层和设计系统。
- 数据模型从首版开始具备多设备标识、修改版本和软删除能力。

### 3.2 当前版本不包含

- Windows 和 macOS 客户端。
- iOS 和 Web 客户端。
- OneDrive、WebDAV 或其他云同步。
- 用户账号、服务端后台和多人协作。
- 系统日历导入、桌面组件和跨设备通知。
- 周报、月报和复杂数据分析。

## 4. 核心用户路径

1. 用户打开应用，默认进入今天。
2. 快速添加一条或多条待办。
3. 标记完成、编辑、删除或查看备注。
4. 在当天任意时间填写复盘内容，应用自动保存。
5. 用户切换日期，查看或补充其他日期的记录。
6. 用户按需配置自己的模型服务并生成当日总结。
7. 应用重启、离线或升级后，数据保持完整。

## 5. 信息架构

### 5.1 手机布局

应用采用三个一级区域：

- 今日：当天进度、待办、复盘和智能总结。
- 日历：按日期查找和进入历史记录。
- 设置：模型服务、外观、数据与隐私信息。

一级导航使用 Material 3 Navigation Bar。进入历史日期后，今日页面结构保持一致，但明确显示当前日期并提供“回到今天”。

### 5.2 平板与宽屏布局

- 使用 Navigation Rail 替代底部导航。
- 日历页可采用日期列表与当天详情双栏布局。
- 内容区域设置合理最大宽度，不将手机单列简单拉伸到全屏。
- 窗口尺寸变化不得造成内容遮挡、控件溢出或状态丢失。

## 6. 功能需求

### FR-01 日期浏览

- 默认选中设备当前时区的今天。
- 支持前一天、后一天、日历选择和回到今天。
- 所有待办、复盘和总结严格绑定本地日历日期。
- 日期持久化格式为 `yyyy-MM-dd`，时间戳使用 UTC Unix 毫秒。
- 跨时区旅行不得自动改变历史记录所属日期。

### FR-02 待办管理

- 标题必填，去除首尾空格后长度为 1 至 80 个字符。
- 备注可选，长度不超过 300 个字符。
- 支持新增、编辑、完成、取消完成和删除。
- 默认排序为未完成优先，同状态下按用户排序值和创建时间排列。
- 删除后立即从界面消失，但数据库保留软删除记录。
- 列表变化应有克制、可中断的过渡动画。

### FR-03 当日进度

- 显示完成数、总数和整数完成率。
- 没有待办时完成率为 0%。
- 完成状态变化后进度立即更新。
- 状态不能只通过颜色表达，必须同时使用图标、文字或形态变化。

### FR-04 每日复盘

- 字段包括：今日亮点、遇到的困难、今日收获、明日最重要的事。
- 每个字段最多 1000 个字符。
- 心情评分为 1 至 5，也可保持未选择状态。
- 输入停止约 500 毫秒后自动保存。
- 应用进入后台、切换日期或关闭页面前必须提交当前草稿。
- 每个日期最多存在一条有效复盘记录。

### FR-05 智能总结

- 支持 OpenAI Chat Completions 兼容接口、Anthropic Messages API 和 Google Gemini API。
- OpenAI 兼容接口允许用户设置 Base URL 和模型名称。
- 请求只包含当前日期的待办标题、完成状态和复盘内容。
- 发送前明确说明内容将提交给用户选择的服务商。
- 总结建议包含：完成概览、今日亮点、值得调整、明日建议。
- 成功结果保存到当前日期；重新生成时覆盖有效版本并更新修改信息。
- 请求失败时保留上次成功结果，错误信息不得包含 API Key。
- 请求超时为 60 秒，生成过程中可取消。

### FR-06 模型配置与密钥

- 应用不内置厂商 API Key。
- 用户可配置服务商、Base URL、模型和 API Key。
- Base URL 默认只允许 HTTPS；开发构建可通过显式调试开关允许本地地址。
- API Key 使用 Android Keystore 支持的安全存储方案保存。
- API Key 不得写入 SQLite、日志、崩溃报告、同步数据或导出文件。
- 设置页允许验证配置、覆盖保存和清除配置。

### FR-07 日历与历史记录

- 日历标记存在待办、复盘或总结的日期。
- 点击日期进入对应日详情。
- 历史数据读取不得依赖网络。
- 当前版本不实现全文搜索和复杂统计。

### FR-08 本地持久化

- 使用 SQLite 保存业务数据，Flutter 数据访问层使用 Drift。
- 数据写入使用事务，并提供明确的数据库 schema 版本。
- 数据库升级必须使用非破坏性迁移，禁止在生产构建中使用清库回退。
- 数据库异常不得导致应用静默丢失全部数据。
- 应用卸载前不主动删除用户数据。

### FR-09 旧版本数据迁移

- 保持 Android application ID `com.limeday.app`。
- 首次启动新版时检测现有 Room `lime_day.db` 数据库。
- 无损迁移旧版 Todo、DailyReview 和 DailySummary 数据。
- 旧版自增 Todo ID 必须转换为新的 UUID，日期和创建时间保持不变。
- 通过一次性 Android 原生迁移桥接读取旧版 Keystore 加密的模型配置，并写入新版安全存储。
- 迁移在单个可恢复流程内执行；失败时保留旧数据库并允许再次尝试。
- 迁移成功并通过数据数量校验后，才允许标记迁移完成。

## 7. 视觉与交互规范

### 7.1 视觉方向

关键词：清新、克制、轻盈、专注、现代。

- 使用 Material 3 作为交互与无障碍基础，建立青柠日记自己的颜色、字体、间距和形状 Token。
- 主色延续薄荷绿，但界面不能被单一绿色覆盖；使用中性色、暖色强调和清晰的状态色形成层次。
- 卡片只用于独立内容单元，不将每个页面区块都设计成浮动卡片。
- 圆角保持适度，常规容器建议 8 至 16dp，避免所有元素均使用大圆角胶囊形态。
- 不使用 Emoji 充当产品图标或功能图标。
- 不使用装饰性渐变球、背景光斑和无功能的大面积插画。

### 7.2 动效

- 页面和状态动画以 150 至 300 毫秒为主。
- 动画服务于状态理解，例如完成待办、列表插入、日期切换和保存反馈。
- 支持系统“减少动态效果”设置。
- 动画不得阻塞输入，也不得导致布局跳动。

### 7.3 Android 体验

- 支持边到边布局并正确处理状态栏、导航栏、键盘和安全区域。
- 支持系统返回手势和预测性返回。
- 触控目标不小于 48dp。
- 支持浅色、深色模式；动态配色可由用户选择启用，不覆盖品牌默认主题。
- 输入法出现时，当前编辑字段必须保持可见。
- 支持系统字体缩放，最大常用字体尺寸下不得出现关键内容截断。

### 7.4 无障碍

- TalkBack 可读出按钮用途、日期、完成状态、进度和表单标签。
- 正文和关键控件满足 WCAG AA 对比度要求。
- 不以颜色作为唯一状态提示。
- 焦点顺序与视觉顺序一致。

## 8. 数据模型

所有可同步业务实体从当前版本开始使用稳定 UUID 和同步元数据，即使本版本尚不连接远端。

### 8.1 TodoItem

| 字段 | 类型 | 说明 |
|---|---|---|
| id | String | UUID 主键 |
| date | String | `yyyy-MM-dd`，建立索引 |
| title | String | 1 至 80 字符 |
| note | String | 0 至 300 字符 |
| isCompleted | Boolean | 完成状态 |
| sortOrder | String | 稳定排序值 |
| createdAt | Int64 | UTC Unix 毫秒 |
| updatedAt | Int64 | UTC Unix 毫秒 |
| deletedAt | Int64? | 软删除时间 |
| deviceId | String | 最后修改设备 UUID |
| revision | Int64 | 单调递增逻辑版本 |

### 8.2 DailyReview

| 字段 | 类型 | 说明 |
|---|---|---|
| id | String | UUID 主键 |
| date | String | `yyyy-MM-dd`，有效记录唯一 |
| highlight | String | 不超过 1000 字符 |
| challenge | String | 不超过 1000 字符 |
| learning | String | 不超过 1000 字符 |
| tomorrowFocus | String | 不超过 1000 字符 |
| mood | Int | 0 至 5 |
| createdAt | Int64 | UTC Unix 毫秒 |
| updatedAt | Int64 | UTC Unix 毫秒 |
| deletedAt | Int64? | 软删除时间 |
| deviceId | String | 最后修改设备 UUID |
| revision | Int64 | 单调递增逻辑版本 |

### 8.3 DailySummary

| 字段 | 类型 | 说明 |
|---|---|---|
| id | String | UUID 主键 |
| date | String | `yyyy-MM-dd`，有效记录唯一 |
| content | String | 模型生成内容 |
| provider | String | 服务商稳定标识 |
| model | String | 生成时模型名称 |
| generatedAt | Int64 | 生成时间 |
| updatedAt | Int64 | 最后修改时间 |
| deletedAt | Int64? | 软删除时间 |
| deviceId | String | 最后修改设备 UUID |
| revision | Int64 | 单调递增逻辑版本 |

### 8.4 AppMetadata

| 字段 | 类型 | 说明 |
|---|---|---|
| deviceId | String | 安装实例 UUID |
| schemaVersion | Int | 业务数据格式版本 |
| legacyMigrationVersion | Int | 旧版迁移状态 |

## 9. 技术方案

### 9.1 基础技术栈

- Flutter Stable + Dart Stable。
- Material 3 + 自定义 Design Tokens。
- Riverpod 管理状态与依赖。
- Drift + SQLite 管理本地业务数据。
- Dio 或经过统一封装的 HTTP Client 处理网络请求。
- `json_serializable` 处理稳定数据序列化。
- Android Keystore 支持的安全存储保存密钥。
- Android 原生桥接仅用于旧数据迁移和 Flutter 暂不支持的系统能力。

依赖版本必须在首次实现时锁定，并通过 lockfile 提交到仓库。

### 9.2 代码组织

```text
lib/
├── app/                  应用入口、路由、主题
├── core/                 时间、错误、日志、通用能力
├── design_system/        Token 与共享组件
├── features/
│   ├── day/              当日待办、复盘、总结
│   ├── calendar/         日期与历史记录
│   └── settings/         模型、外观、隐私
├── data/
│   ├── local/            Drift 数据库与迁移
│   └── repositories/     Repository 实现
├── domain/               平台无关模型与规则
├── llm/                  服务商适配器
└── sync/                 未来同步协议边界，当前不实现远端
```

### 9.3 架构约束

- UI 不直接执行 SQL、HTTP 或安全存储操作。
- Repository 接口位于平台无关层。
- LLM 服务商使用统一接口和独立 Adapter。
- Android `Context`、Activity、MethodChannel 类型不得进入领域模型。
- 不为简单 CRUD 引入多层空转的 UseCase 类。
- 业务错误使用稳定错误类型，界面负责转换为用户可理解的提示。
- 日志默认不得包含日记正文、API Key、Authorization Header 或完整模型响应。

### 9.4 未来同步边界

当前版本只定义以下接口概念，不实现 OneDrive 或 WebDAV 网络功能：

- `SyncEngine`：计算本地变更、合并远端变更。
- `SyncBackend`：列出、读取、条件写入和删除远端对象。
- `SyncEnvelope`：带 schema 版本、设备和加密信息的数据载体。
- `ConflictResolver`：根据 revision、逻辑时钟和设备标识合并记录。

禁止将 SQLite 数据库文件作为未来同步文件。未来同步内容必须在上传前完成端到端加密，API Key 和 OAuth Token 永不进入同步数据。

## 10. 性能与稳定性

- 中端 Android 设备正常冷启动目标不超过 2 秒。
- 今日页面本地数据读取目标不超过 200 毫秒。
- 常规滚动、完成动画和页面切换目标保持流畅，不进行同步阻塞 I/O。
- 自动保存不得造成明显输入卡顿。
- 网络失败、模型响应异常和数据库写入失败均提供可恢复状态。
- 所有异步操作在页面销毁后不得继续更新已失效的 UI 状态。

## 11. 测试要求

- Domain 和 Repository 规则使用单元测试覆盖。
- Drift migration 覆盖空数据库、旧 Room v1、旧 Room v2 和当前 schema 升级。
- Riverpod 状态覆盖日期切换、自动保存、Todo 操作和总结状态。
- 关键页面提供 Widget Test。
- 旧版数据迁移提供 Android 集成测试和迁移前后记录数量校验。
- 三类 LLM Adapter 使用固定响应样本测试解析与错误脱敏。
- 至少在 API 26 和当前 targetSdk 对应模拟器验证启动与核心流程。
- 浅色、深色、字体放大和手机/平板宽度进行截图回归检查。

## 12. 构建与发布

- application ID 保持为 `com.limeday.app`。
- targetSdk 和 compileSdk 使用项目开发时稳定可用的最新 Android SDK，当前基线为 36。
- minSdk 保持 26。
- 新版本 versionCode 必须大于现有版本 2。
- Debug 和 Release 使用不同的日志和调试配置。
- Release 构建关闭敏感日志，不包含测试密钥和调试服务器地址。
- 正式发布前完成签名、APK/AAB 校验、依赖许可证检查和凭据扫描。

## 13. 当前版本验收标准

1. Flutter Android 应用可在 API 26 和 API 36 安装启动，无崩溃。
2. 旧版 Android 数据和可迁移的模型配置在升级后完整保留。
3. 新增、编辑、完成、删除待办后，列表与进度即时更新。
4. 不同日期的数据互不串联，日历能标记存在记录的日期。
5. 复盘输入自动保存，切换日期、进入后台和重启应用后内容不丢失。
6. 三类 LLM 接口可配置、生成和保存总结，失败时保留旧结果。
7. 无网络时除智能总结外的全部功能正常使用。
8. 浅色、深色、系统字体放大和手机/平板布局无关键内容遮挡。
9. TalkBack 能正确读出主要导航、按钮、进度、完成状态和输入字段。
10. 数据库中不存在 API Key，日志和错误信息不泄露密钥或日记正文。

## 14. 后续路线

### 阶段 2：Android 体验完善

- 系统提醒与可选桌面组件。
- 搜索、周/月趋势和手动数据导入导出。
- 后台任务与电量策略优化。

### 阶段 3：多端同步

- 实现端到端加密同步协议。
- 优先支持 WebDAV。
- 随后支持 OneDrive Microsoft Graph。
- 增加同步状态、冲突处理、恢复密钥和同步诊断。

### 阶段 4：桌面客户端

- 在同一仓库增加 Windows 和 macOS Flutter target。
- 复用领域模型、数据库 schema、同步协议、Repository 和 Design Tokens。
- 根据桌面交互习惯增加侧栏、快捷键、右键菜单、窗口状态和系统安全存储适配。
