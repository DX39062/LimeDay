# 青柠日记（暂定名）产品规格说明书

版本：1.1（LLM 总结版）
状态：可开发
目标平台：Android，`compileSdk/targetSdk 36`（Android 16），最低 Android 8.0（API 26）

## 1. 产品概述

青柠日记是一款离线优先的每日待办与复盘应用。用户围绕某一天建立待办、逐项完成，并在同一日期下记录成果、困难、收获、明日计划和心情，让“计划—行动—复盘”形成一个低负担闭环。

### 1.1 产品目标

- 用户在 30 秒内完成当天计划录入。
- 用户能直观看到当天完成数量与完成率。
- 用户在当天结束时用结构化问题完成复盘。
- 无账号、无网络时全部核心功能可用，数据仅保存在设备本地。

### 1.2 非目标（MVP 不包含）

- 登录、云同步、多人协作。
- 系统通知、桌面组件、日历导入。
- 周报/月报、AI 总结、数据导出。
- iOS、Web 与平板专属布局。

## 2. 目标用户与核心场景

目标用户是希望轻量管理每天任务，同时培养复盘习惯的学生、知识工作者与自由职业者。

核心路径：打开应用 → 查看/切换日期 → 添加待办 → 标记完成 → 填写复盘 → 自动保存 → 次日回看。

## 3. 信息架构

应用采用单页纵向结构，避免在“计划”和“复盘”之间反复切页：

1. 顶部：产品问候、当前日期、上一天/回到今天/下一天。
2. 今日进度：完成数、总数与完成率。
3. 每日待办：快速新增、完成切换、编辑、删除。
4. 每日复盘：今日亮点、遇到的困难、今日收获、明日最重要的事、心情评分。
5. 智能总结：根据所选日期的待办完成情况和复盘内容生成结构化总结。

## 4. 功能需求

### FR-01 日期浏览

- 默认选中设备本地时区的今天。
- 支持前后按天切换，非今天时显示“回到今天”。
- 所有待办和复盘严格绑定所选日期；切换日期后内容同步更新。

### FR-02 待办管理

- 标题必填，去除首尾空格后长度为 1–80 个字符。
- 可选备注，长度不超过 300 个字符。
- 支持新增、编辑、删除、完成/取消完成。
- 列表按“未完成优先，其次创建时间”排列。
- 删除立即生效；MVP 不提供回收站。

### FR-03 进度反馈

- 显示“已完成 / 总数”和整数完成率。
- 没有待办时完成率为 0%，显示轻量空状态引导。
- 完成状态变化后进度即时更新。

### FR-04 每日复盘

- 字段：今日亮点、遇到的困难、今日收获、明日最重要的事；每项最多 1000 字。
- 心情评分为 1–5，可不选（0）。
- 输入停止约 500ms 后自动保存；离开页面或切换日期前的数据不得丢失。
- 每个日期最多一条复盘记录。

### FR-05 本地持久化

- 使用 Room/SQLite 保存数据，应用重启后完整恢复。
- 待办删除时只删除目标记录，不影响同日复盘。
- 数据库升级必须提供迁移策略；开发期首版 schema 版本为 1。

### FR-06 大语言模型总结

- 支持 OpenAI 兼容接口、Anthropic Messages API、Google Gemini GenerateContent API。
- OpenAI 兼容接口允许自定义 Base URL 和模型，兼容 OpenAI、DeepSeek、Moonshot、通义等采用 Chat Completions 协议的服务。
- 用户可配置提供商、Base URL、模型和 API Key；API Key 使用 Android Keystore 加密后仅存于本机。
- 生成请求只包含当前日期的待办标题、完成状态和复盘内容；发送前明确提示数据将提交给用户选择的服务商。
- 生成结果保存到所选日期，可重新生成覆盖；失败时保留上次成功结果。
- 请求超时为 60 秒；错误信息不得包含 API Key。
- 总结建议包含：完成概览、今日亮点、值得调整、明日建议；模型输出作为辅助内容，不作为医疗或专业决策依据。

### FR-07 LLM 配置与隐私

- 首次使用智能总结时引导完成配置，不内置任何厂商密钥。
- API Key 输入默认隐藏，配置界面可覆盖保存。
- 禁止将密钥写入日志、数据库、崩溃信息或源码。
- 应用备份关闭，避免加密配置在没有对应 Keystore 密钥的设备上恢复。

## 5. 视觉与交互规范

- 风格关键词：清新、克制、温和、留白。
- 主色：薄荷绿 `#2E7D6E`；浅背景 `#F6FAF8`；强调色 `#F4B860`；正文 `#1C2B27`。
- 卡片圆角 20dp，页面水平边距 20dp，触控目标不小于 48dp。
- 标题使用较强字重，正文保持高对比度；不以颜色作为唯一状态提示。
- 支持系统浅色/深色模式和动态字体；输入框必须有可读标签。
- 完成待办以勾选图标、删除线和弱化文字共同表达。

## 6. 数据模型

### TodoItem

| 字段 | 类型 | 约束 |
|---|---|---|
| id | Long | 主键，自增 |
| date | String | `yyyy-MM-dd`，建索引 |
| title | String | 1–80 字符 |
| note | String | 0–300 字符 |
| isCompleted | Boolean | 默认 false |
| createdAt | Long | Unix 毫秒 |

### DailyReview

| 字段 | 类型 | 约束 |
|---|---|---|
| date | String | 主键，`yyyy-MM-dd` |
| highlight | String | ≤1000 字符 |
| challenge | String | ≤1000 字符 |
| learning | String | ≤1000 字符 |
| tomorrowFocus | String | ≤1000 字符 |
| mood | Int | 0–5，0 表示未选 |
| updatedAt | Long | Unix 毫秒 |

### DailySummary

| 字段 | 类型 | 约束 |
|---|---|---|
| date | String | 主键，`yyyy-MM-dd` |
| content | String | 模型生成内容 |
| provider | String | 生成时使用的提供商 |
| model | String | 生成时使用的模型 |
| generatedAt | Long | Unix 毫秒 |

## 7. 技术方案

- Kotlin + Jetpack Compose + Material 3。
- 单 Activity，MVVM；`StateFlow` 驱动单向数据流。
- Room 数据库 + Repository；协程执行写入；提供 1→2 的非破坏性迁移。
- OkHttp 统一网络层，提供商适配器负责各类鉴权、请求体与响应解析。
- Android Keystore AES/GCM 加密 LLM 配置，不申请外部存储权限。
- `LocalDate` 作为 UI 日期类型，持久层以 ISO 字符串存储。
- Android 16：`compileSdk = 36`、`targetSdk = 36`，采用边到边布局并处理系统栏 Insets。

## 8. 验收标准

1. Android 16 模拟器/设备可安装并启动，无崩溃。
2. 新增三条待办、完成一条后显示 `1 / 3` 与 `33%`。
3. 编辑、删除、取消完成均立即反映到列表和进度。
4. 昨天、今天、明天的数据互不串联。
5. 输入复盘并重启应用后，所选日期内容仍存在。
6. 空标题不能创建待办，超长内容在 UI 层被限制。
7. 浅色、深色模式下文字和控件清晰可见。
8. TalkBack 能读出按钮用途、待办完成状态及复盘字段标签。
9. 三类 LLM 配置能生成并保存总结，HTTP/解析失败时显示可恢复错误。
10. APK 中不包含 API Key，应用图标和产品界面不使用 Emoji 作为产品图形。

## 9. 后续版本建议

- V1.1：提醒、桌面小组件、复制昨日未完成任务。
- V1.2：周/月趋势、搜索、Markdown/数据导出。
- V2.0：可选端到端加密同步与跨平台客户端。
