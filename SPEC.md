# 青柠日记产品与技术规格说明书

- 版本：2.7.2
- 状态：2.7.2 视觉与快速录入实现契约（以已发布的 2.7.1 为稳定基线）
- 当前目标：Android 原生客户端
- 目标架构：ARM64 (`arm64-v8a`)
- 应用标识：`com.limeday.app`

## 1. 产品定位

青柠日记是一款离线优先、轻量、现代的每日计划与复盘应用。用户先围绕某一天安排和完成待办，再主动进入该日期的复盘页面记录经验，并可按需调用自己的大语言模型服务生成总结。

当前阶段只交付 Android 原生客户端。Windows、macOS 和其他平台后续再加入；本阶段不为了共享 UI 引入跨平台框架，但数据模型与同步文件格式必须保持平台无关。

## 2. 产品原则

- 打开应用即可记录，无强制账号或登录。
- 本地数据库是可信数据源，离线时待办与复盘完整可用。
- 待办是每日主页的核心，复盘是用户主动进入的二级流程。
- 界面使用原生 Android 交互与 Material 3 布局能力，但应用内可见图标采用统一自绘的记事本涂鸦语言，不直接暴露 Android 默认图标风格。
- 用户明确知道哪些内容留在本机，哪些内容会发送到 WebDAV 或模型服务商。
- 同步失败不得阻塞本地使用，也不得静默覆盖较新的数据。

## 3. 当前版本范围

### 3.1 必须实现

- Kotlin、Jetpack Compose、Material 3 原生 Android 应用。
- 日期切换、每日待办、完成进度、每日复盘和 LLM 总结。
- 计划日期与截止日期分离；待办支持截止时间、单项提醒、重复规则、一级分组、步骤、搜索、逾期与计划中视图。
- 复盘作为每日待办页的二级入口，不在待办列表下方直接展示复盘表单或总结。
- 每日 LLM 总结入口和结果位于复盘页面；模型服务管理位于独立二级页面。
- 独立总结页支持本周、本月、本季度和自定义日期范围，并保留生成历史。
- 底部导航固定为“待办、总结、设置”三个一级页面；待办和总结不再保留顶部设置快捷入口。
- 自适应涂鸦应用图标、手绘底部导航与操作图标，以及不挤占内容高度的待办页顶部装饰。
- 修复搜索首次展开时的焦点竞态；快速新增在加号左侧提供仅图标的分组选择，并为分组提供稳定的手绘图标库。
- 模型服务按官方 Logo 核心轮廓绘制可识别的夸张涂鸦版本；全应用自有功能图标不得回退到 Android/Material 原生风格。
- `MainActivity` 明确退出 Android 预测返回动画；侧边拖动期间页面完全不位移、不缩放、不淡出，取消手势时页面、滚动、弹窗和草稿保持原状，提交后才返回一次。
- WebDAV 配置、连接测试、手动同步与系统后台定期同步。
- Room 1/2 版和 Flutter/Drift 3 版数据库的非破坏性升级。
- 仅生成和验证 ARM64 APK。

### 3.2 暂不实现

- Windows、macOS、iOS 和 Web 客户端。
- OneDrive、厂商账号和自建同步服务。
- 多人协作、共享清单和服务端推送。
- 全文搜索、系统日历导入和桌面组件。
- 附件、协作、标签和多级嵌套分组。
- 不保留墓碑的直接物理删除；永久删除正文后仍保留最小同步墓碑。

## 4. 信息架构

### 4.1 每日待办页

应用启动后默认进入今天的待办页：

1. 顶部应用栏：产品名与轻量青柠涂鸦装饰，不显示设置入口。
2. 日期导航：前一天、可点击日期、后一天和回到今天；点击日期打开当前所选月份的月历并快速跳转。
3. 当日进度：仅在“当天”且没有搜索时显示 64–72dp 的紧凑卡片；同一行显示“完成 7 / 8”和较小的“87%”，进度条约 5dp，不再显示大号“完成进度”标题。
4. 待办输入与列表：新增、完成、编辑、优先级、计划日期、截止时间、提醒、重复、分组、步骤、复制和删除；快速新增的加号左侧显示仅图标的当前分组按钮，选择后将目标 `groupId` 写入新待办。
5. 视图与检索：当天、逾期、计划中三种视图；同一行放置 48dp 自绘搜索与分组按钮。搜索按钮展开约 48dp 高的紧凑搜索框，覆盖标题、备注和步骤；首次未聚焦回调不得误折叠，只有真正获得过焦点后失焦且查询为空才自动折叠，关闭时清空并恢复进入搜索前的视图。
6. 复盘入口：显示“开始复盘”或“继续复盘”，可展示是否已有复盘/总结，但不得直接展示复盘正文。

### 4.2 每日复盘页

- 顶部显示当前日期并提供原生返回导航。
- 在复盘字段之前显示当日全部待办，未完成项优先。
- 待办支持完成、取消完成、点击事项编辑、更多操作和可收回的左滑删除操作，状态变化实时影响当日进度与智能总结输入。
- 复盘只提供“解决了什么问题？”和“随便写写”两个文字区域，不显示心情评分。
- 输入停止约 500 毫秒后自动保存；离开页面时立即提交草稿。
- 已启用智能总结时，LLM 总结区域位于复盘表单之后；关闭时不渲染生成入口，已有每日总结可只读查看。
- 未配置模型时，从本页打开模型配置；配置完成后仍停留在本页。
- 生成前说明当日待办与复盘内容会发送给所选服务商。

### 4.3 设置页

- 作为“待办、总结、设置”底部导航中的一级页面，不显示返回按钮。
- 待办设置：待办提醒、复盘提醒和回收站。
- 总结设置：模型服务与本机提示词偏好；模型服务继续使用独立二级页面。
- 总结设置顶部提供“启用智能总结”总开关；新安装默认关闭，升级时已有模型服务的用户保持开启。
- 通用设置：外观、数据导入导出、WebDAV 和关于。

- 外观模式：跟随系统、浅色和深色。
- 每日待办提醒与每日复盘提醒，可分别启用并设置时间。
- 本地数据导出与导入；导入采用与同步一致的冲突合并规则，且不包含任何凭据。
- 回收站入口显示已删除待办数量；回收站二级页支持按删除时间查看和恢复待办。
- 回收站支持选择、全选、恢复所选、永久删除所选与清空；永久删除前必须二次确认。
- 设置主页只显示 WebDAV 配置状态和最近同步信息，点击后进入 WebDAV 二级页。
- WebDAV 二级页提供服务地址、用户名、密码、远端目录、保存、测试、立即同步和清除配置。
- 关于区域显示应用版本、数据与隐私说明和开源许可入口。
- 密码与 LLM API Key 均不进入数据库、同步文件和日志。

### 4.4 总结页

- 作为与每日待办并列的底部导航主页面，不隐藏在设置或每日复盘中。
- 智能总结关闭时仍保留页面和历史，只读显示已有总结并隐藏所有生成控制。
- 提供本周、本月、本季度和自定义日期范围；单次范围最多 93 天。
- 默认读取范围内的原始待办和复盘；用户可额外选择包含已有每日总结。
- 支持选择已保存的供应商和临时覆盖模型，不修改全局默认配置。
- 支持快捷指令、自由输入、收藏指令和最近使用记录。
- 每次生成都创建一条历史记录，同一范围重新生成不得覆盖旧结果。
- 总结历史默认逐条折叠，只显示日期范围、供应商/模型和内容首行；点击卡片展开正文，展开状态不跨进程保存。
- 顶部不显示模型设置按钮，所有总结设置统一从底部“设置”进入。

### 4.5 模型服务页

- 从设置页或每日总结区域进入独立二级页面。
- 以卡片展示多个本地加密供应商，明确标记当前默认供应商。
- 同一厂商允许保存多份独立配置；界面统一称“模型服务”，不使用容易与付费计划混淆的“订阅”。
- 支持添加、编辑、复制、删除、启用、上移、下移、连接测试和模型列表刷新。
- 顶部右侧始终显示明确尺寸的自绘加号和“新增”文字，列表底部始终显示宽按钮“新增模型服务”；两处入口语义与行为一致，不得因 Canvas 无尺寸而不可见。
- 点击卡片主体直接编辑，右侧同时固定显示自绘编辑按钮；测试、默认与更多操作使用可换行布局，系统字体放大时不得被裁切。
- 添加时先打开供应商预设选择，再进入编辑器，不得静默默认成 OpenAI；每次保存新服务都生成新 UUID，不能覆盖当前服务。
- 首次添加时，在保存前明确提供“手动获取模型”按钮；只有用户点击后才向配置的端点发起模型列表请求。
- 模型刷新失败不阻止手动填写模型或保存供应商。
- 每个内置 `presetId` 使用基于当前官方 Logo 核心轮廓重新绘制的独立夸张手绘图标；同一厂商多份配置共用图标，以名称、模型和默认状态区分。

## 5. 功能需求

### FR-01 日期浏览

- 默认选择设备本地时区的今天。
- 支持前一天、后一天和回到今天。
- 点击中间日期卡片打开月历，初始显示所选日期所在月份；点击某天后立即跳转并关闭月历。
- 月历提供“今天”快捷操作；有待办的日期显示圆点，全部完成显示实心圆点，状态不能只依赖颜色表达。
- 数据所属日期固定保存为 `yyyy-MM-dd`。
- 切换日期前提交当前复盘草稿。

### FR-02 待办管理

- 标题去除首尾空格后为 1 至 80 个字符。
- 备注可选，不超过 300 个字符。
- 支持新增、编辑、完成、取消完成和删除。
- `date` 表示计划出现日期；可选 `dueDate`/`dueTime` 表示截止日期与本地时间，两者不得互相覆盖。
- 只有截止日期时不隐式写入 `23:59`；截止时间按创建时区解析并保存 UTC 毫秒与时区 ID。
- 可选单项提醒独立于截止时间，支持截止时、提前 10 分钟、1 小时、1 天和自定义本地时间；精确调度不可用时允许系统降级延迟，不要求特殊精确闹钟权限。
- 未完成且截止已过的事项保留原计划日期，并聚合显示在当前日“逾期”区域；逾期状态通过文字、图形和颜色共同表达。
- 重复规则支持每天、工作日、每周、每月与自定义间隔；完成当前实例后生成下一条独立未完成记录，历史实例不被改写。
- 每条待办属于一个一级分组；旧数据和未指定分组归入固定 ID 的内置“日常”。从 2.7.0 升级时，仅把该固定分组且名称恰为“收件箱”的记录幂等改名为“日常”，保留 ID、关联待办和同步数据；分组可选择项目自绘图标与低饱和颜色，可排序、重命名和软删除，不支持嵌套。
- 分组图标来自版本化的项目手绘图标库；“日常”固定使用清单与小太阳。新分组优先分配未使用图标，全部用完后允许循环复用，用户可在创建或编辑时更换图标和颜色。
- 2.7.1 及更早仍使用通用 `folder`、`leaf` 或空图标键的普通分组，只在 2.7.2 首次规范化时按稳定顺序分配图标；不得覆盖明确图标，重复启动不得再次写入或递增 revision。
- 一条待办可包含多个可排序步骤；步骤可独立勾选，列表展示完成数/总数，主待办完成不会静默删除步骤。
- 本地搜索覆盖待办标题、备注和步骤；智能视图提供“逾期”和“计划中”，不新增底部导航。
- 搜索展开必须等待输入节点进入组合后请求焦点；首次 `isFocused = false` 不表示失焦。非空搜索失焦保留结果，输入法完成只隐藏键盘，关闭才清空并恢复进入搜索前的智能视图。
- 快速新增的分组按钮位于加号左侧，只显示当前分组手绘图形与颜色；点击浮层列出有效分组并在底部提供“管理分组”。新增成功后在当前页面会话保留选择，进程重启或选择失效时回到“日常”。
- 列表不显示常驻编辑或删除图标；点击事项正文进入编辑。
- 每条待办提供自绘“更多”按钮，可设置高/普通/低优先级、计划日期、截止与提醒、重复、分组、步骤、复制待办或移到回收站。
- 优先级只通过文字和图形共同提示，不自动改变列表顺序；旧数据和未设置优先级的数据默认为普通。
- 移动日期保留标题、备注、完成状态、优先级、创建时间与稳定排序值，并更新同步元数据。
- 复制待办在原日期创建新的未完成事项，复制标题、备注和优先级，使用新的 UUID、创建时间和稳定排序值。
- 左滑只露出固定宽度的“移到回收站”按钮，不得因滑动距离直接删除；用户必须再次点击该按钮。
- 展开的左滑操作可通过右滑、点击事项正文或点击其他待办收回；同一列表最多展开一条，离开页面后不保留展开状态。
- 编辑弹窗保留“移到回收站”入口，并在执行前要求确认，供 TalkBack、键盘和无法使用滑动手势的用户操作。
- 每日待办页与每日复盘页采用一致的待办操作和删除反馈。
- 未完成项优先，同状态按稳定排序值和创建时间排列。
- 删除后从界面消失，数据库保留软删除记录，并在底部导航上方提供 6 秒“撤销”操作。
- 6 秒内连续删除多条时合并为同一批次，提示“已移入回收站 N 项”，一次撤销恢复该批全部事项；提示不得被底部导航遮挡。
- 回收站展示所有日期的软删除待办，按删除时间倒序排列，并支持恢复到原日期。
- 回收站支持单选、多选、全选、恢复所选、永久删除所选和清空。
- 永久删除移除标题、备注、时间、分组引用和步骤正文，但必须保留只含实体 ID、删除时间、revision 与 deviceId 的 TodoTombstone，并参与同步以阻止旧设备复活记录。
- 永久删除与清空均需二次确认，执行后不提供瞬时撤销；当前版本不自动清理墓碑。
- 每次修改更新 `updatedAt`、`deviceId`，并递增 `revision`。

### FR-03 当日进度

- 显示完成数、总数和整数完成率。
- 没有待办时完成率为 0%。
- 完成状态不能只依赖颜色表达。
- 仅在当天视图且搜索为空时展示；逾期、计划中和搜索结果不得误用所选日期总数。
- 卡片总高目标为 64–72dp，完成数与百分比同行，进度条约 5dp，不显示冗余的大号分区标题。

### FR-04 每日复盘

- 字段仅为“解决了什么问题？”和“随便写写”；前者最多 1000 个字符，后者最多 3200 个字符，以无损容纳旧版三个字段及其原标签。
- “解决了什么问题？”默认显示约 4 行，“随便写写”默认显示约 8 行，长内容在输入区域内滚动。
- 新内容分别复用 `challenge` 和 `highlight` 字段保存，以保持数据库字段与 WebDAV v1→v2 迁移兼容。
- 旧记录中的“遇到的困难”映射到“解决了什么问题？”。
- 旧记录中的“今日亮点、今日收获、明日重点”按原标签合并显示到“随便写写”；用户第一次编辑后完整保存到 `highlight`，并清空已合并的 `learning` 与 `tomorrowFocus`。
- 旧心情数据继续保留在同步记录中，但新界面不显示或修改心情评分。
- 每个日期最多一条有效复盘。
- 自动保存、后台切换和返回不得丢失已输入内容。

### FR-05 智能总结

- 设置提供全局 `llmEnabled` 开关。新安装默认关闭；从 2.6 升级且已保存至少一个模型服务时首次迁移为开启。
- 关闭时每日复盘与范围总结都不得发起 LLM 请求；复盘页隐藏生成入口，总结页隐藏生成控件，但已有每日/范围历史继续只读可见，模型服务配置保持可管理。

- 支持 OpenAI Chat Completions、OpenAI Responses、Anthropic Messages 和 Google Gemini Native 四类协议。
- 内置 OpenAI、Anthropic、Gemini、OpenRouter、DeepSeek、Kimi、通义千问、智谱 GLM、SiliconFlow、MiniMax、豆包、xAI、Mistral、Groq、Ollama和自定义 OpenAI 兼容预设。
- Azure OpenAI、AWS Bedrock 与 Google Vertex AI 因认证方式不同，不在当前 2.7 范围内。
- 保存多个供应商并选择一个全局默认供应商；每日总结与范围总结均可临时覆盖供应商和模型。
- 模型列表由用户显式刷新，成功结果加密缓存在本机 24 小时；OpenAI、Anthropic、Gemini 分别使用对应协议的模型列表接口。
- 新建服务编辑器在尚未保存时也提供显眼的“手动获取模型”操作；端点与必要凭据未填写时禁用，并说明请求会发送到当前服务端点。
- 获取模型失败时显示可恢复错误，保留手动输入并允许保存。
- 提供不产生总结记录的连接测试；不得把 API Key 写入错误信息。
- 请求仅包含当前日期的待办标题、完成状态和复盘内容。
- 结果保存到当前日期，重新生成时覆盖有效版本并更新同步元数据。
- 每日生成支持“总结今日进展”“给出明日建议”“分析未完成事项”“压缩成三句话”等快捷指令和一次性自由指令，不实现多轮聊天。
- 用户可收藏常用指令；最近使用记录最多保存 10 条，收藏与历史只保存在本机加密配置中。
- 失败时保留上次成功结果，错误不得泄露 API Key。
- 请求超时 60 秒，生成过程可取消。
- 生成与重新生成按钮使用右对齐的紧凑尺寸，触控目标仍不得小于 48dp。

### FR-06 密钥安全

- 应用不内置 API Key、WebDAV 密码或发布密钥。
- LLM 配置与 WebDAV 凭据使用 Android Keystore AES/GCM 加密后保存在本机。
- 供应商列表、模型缓存、收藏指令和最近指令作为同一加密设置保存；旧版单供应商配置首次读取时无损迁移。
- 密钥不得写入 Room、日志、崩溃信息、同步 JSON 或导出文件。
- Base URL 默认必须使用 HTTPS。Ollama 或局域网 HTTP 仅在用户为该供应商明确开启“不安全的本地 HTTP”后允许，并在界面持续提示风险。
- 应用关闭 Android 自动备份，避免密文与 Keystore 分离恢复。

### FR-07 WebDAV 同步

- 用户配置 HTTPS WebDAV 根地址、用户名、密码和远端目录。
- 默认远端文件升级为 `<目录>/limeday-sync-v2.json`；首次升级时如 v2 不存在，可读取 v1 后迁移并创建 v2，后续只写 v2。
- 保存前规范化地址和目录，默认仅允许 HTTPS。
- 连接测试不得修改业务数据。
- 手动同步流程为：下载远端快照、逐实体合并、事务写入本地、上传合并快照。
- 远端文件不存在时，首次同步创建目录和文件。
- v2 同步文件包含待办、分组、步骤、待办墓碑、复盘、每日总结、范围总结、格式版本和生成时间，不包含任何凭据。
- 新客户端可读取 v1 并补齐默认值；v2 文件不得被 2.6 及更早客户端当作 v1 覆盖。未知版本继续拒绝合并。
- 后台同步使用 WorkManager，仅在网络可用时运行；失败按系统策略重试。
- HTTP、认证、解析或上传失败时保留本地数据，并显示可恢复错误。

### FR-08 冲突解决

- Todo、TodoGroup、TodoStep、TodoTombstone 与 RangeSummary 以 UUID 为身份；DailyReview 和 DailySummary 以日期为逻辑身份。
- 同一身份的两个版本依次比较 `updatedAt`、`revision`、`deviceId`，较大者胜出。
- 软删除记录参与同样比较，避免旧设备复活已删除内容。
- 同一 Todo ID 同时存在正文和墓碑时先比较版本；墓碑不旧于正文时正文不得恢复。清空回收站只压缩正文，不丢弃墓碑。
- 每次上传包含合并后的完整快照；未知格式版本必须拒绝合并。
- 同步操作应具备幂等性，连续同步不得产生重复记录。

### FR-09 数据迁移

- 保持 application ID `com.limeday.app` 和数据库名 `lime_day.db`。
- 支持 Room schema 1/2 原生旧版升级。
- 支持 Flutter/Drift schema 3 版本回迁到原生 Room。
- 旧自增 Todo ID 转换为稳定 UUID；日期、正文、完成状态和时间保持不变。
- Room schema 4 升级到 schema 5 时为待办增加优先级字段，已有记录全部设为普通。
- Room schema 5 升级到 schema 6 时新增范围总结表，不改变已有待办、复盘和每日总结。
- Room schema 6 升级到 schema 7 时新增 `todo_groups`、`todo_steps`、`todo_tombstones`，并为 Todo 增加截止、时区、提醒、重复与分组字段；所有旧待办指向迁移生成的固定默认分组“日常”。
- 升级不得使用破坏性清库回退。
- 迁移必须保留待办、复盘、总结及旧版 Keystore 中的 LLM 配置。

### FR-10 外观设置

- 支持跟随系统、浅色和深色三种模式，默认跟随系统。
- 选择后立即生效，并在应用重启后保持。
- 外观设置仅保存在本机，不进入业务数据库或同步快照。

### FR-11 每日提醒

- 待办提醒和复盘提醒可分别启用，并分别设置本地时间。
- 默认时间为待办 `09:00`、复盘 `21:00`，默认均关闭。
- Android 13 及以上仅在用户启用提醒时申请通知权限；拒绝权限后不得反复弹出系统授权框。
- 提醒使用设备本地时区，由 WorkManager 调度；设备重启或时区变化后仍由系统恢复调度。
- 通知点击后打开应用；提醒失败不得影响本地待办与复盘使用。
- 单条待办提醒使用独立唯一任务调度；编辑、完成、删除、恢复、永久删除和重复生成后必须取消或重排对应提醒。

### FR-12 数据导入导出

- 用户通过系统文件选择器导出 JSON 备份或选择 JSON 文件导入。
- 导出包含待办、复盘、每日总结、范围总结及同步元数据，不包含 WebDAV 密码、LLM API Key、供应商端点或本机设置。
- 导入先完整解析并校验格式，再按 `updatedAt`、`revision`、`deviceId` 与本地数据合并。
- 不支持的格式、损坏文件或超过大小限制的文件必须拒绝，且不得部分写入数据库。
- 导入和导出结果在设置页向用户明确反馈。

### FR-13 范围总结

- 本周按周一至周日、本月按自然月、本季度按自然季度计算，自定义范围包含起止日期。
- 范围必须起止有序且不超过 93 天；没有待办、复盘或被选择的已有总结时不得发起请求。
- 默认输入为有效待办和有效复盘，不复用旧 LLM 总结；只有用户主动勾选后才加入已有每日总结。
- 输入过长时按日期边界自动分段总结，再进行一次最终综合；分段中间结果不写入数据库。
- 每次成功生成创建独立 RangeSummary；历史支持折叠查看、展开正文和软删除，不覆盖同范围旧版本。
- 历史记录保存范围、周期类型、提示词、内容、供应商 ID/显示名、模型、是否包含已有总结和同步元数据。
- 范围总结参与 WebDAV 和 JSON 备份；供应商凭据、端点和模型缓存不参与。

### FR-14 供应商管理

- 预设只填充名称、协议、官方 Base URL、默认模型和可选模型列表地址，不内置 API Key。
- 当前默认供应商以状态标签和边框共同标识；切换默认供应商不修改其他供应商。
- 允许保存多个供应商，也允许同一预设保存多份不同名称、端点、模型或 API Key 的配置。
- 卡片主体和固定自绘铅笔按钮均可进入编辑；编辑动作不得仅存在于可能溢出或被裁切的横向按钮行。
- 删除必须确认；删除当前默认供应商时自动选择剩余列表首项，列表为空时进入未配置状态。
- 复制生成新的 UUID 和“副本”名称，保留协议、端点、模型及 API Key，并插入原供应商之后。
- 排序只改变本机显示顺序，不影响已保存总结或同步数据。
- 供应商图标按当前官方 Logo 的核心轮廓和代表色，用 Compose Canvas/项目 VectorDrawable 重新绘制为明显不规则、错位填色的夸张涂鸦版本；不直接嵌入官方位图、第三方图标包或 Android 内置图标。未知/自定义预设使用原创插头与对话框 fallback。
- 新增入口必须同时存在于页头和列表尾部，使用明确尺寸与 48dp 触控目标；点击后先选预设再编辑。已有服务时新增服务必须保留旧列表和默认选择。

### FR-15 导航、图标与涂鸦视觉

- 一级底部导航固定为“待办、总结、设置”，切换时保留各页面滚动与输入状态。
- `MainActivity` 设置 `android:enableOnBackInvokedCallback="false"`，完全禁用预测返回预览。应用内返回仍由 `OnBackPressedCallback`/Compose `BackHandler` 在提交后处理：拖动期间页面不位移、不缩放、不淡出、不弹栈、不触发保存或销毁；取消时状态完全不变，提交后只返回一次。待办根页提交返回后退出应用，但同样不显示系统预测预览。
- 总结和设置一级页返回时先回到待办；待办一级页再次返回才退出应用。弹窗存在时返回只关闭最上层弹窗。
- 外层主导航 Scaffold 不消费顶部系统栏，页面级顶部栏各自只消费一次；标题与状态栏有效内容之间目标间距约 10dp，不能绘制到刘海或挖孔区域。
- 设置作为一级页时无返回按钮；模型服务、WebDAV、回收站等二级页保留自绘返回按钮。
- 应用内的导航、日期、设置、日历、添加、删除、编辑、更多、复制、刷新、收藏、模型测试、默认服务、展开收起、WebDAV、导入导出、提醒和关于图标全部由 Compose Canvas 或项目 VectorDrawable 自绘，不直接渲染 Material Icons。
- 应用源码不得使用 `androidx.compose.material.icons`、`Icons.Default/Filled/Outlined`、`android.R.drawable`，也不得把 Emoji 或 Unicode 箭头用作功能图标；Material 3 仅保留布局、交互和语义基础。
- 待办三点菜单中的“截止、提醒、分组和步骤”使用清单/调节图形，“移动到其他日期”使用日历加右箭头；移动、复制、删除、优先级等不同语义不得复用同一个图形。
- 系统状态栏、输入法和系统日期选择器内部控件不属于应用可重绘范围；应用自己提供的入口、快捷键与状态标识仍必须自绘。
- 自绘图标采用深绿色不规则单线、轻微错位与少量柠檬黄填色；保证 48dp 触控目标、TalkBack 描述、深浅色对比和图形之外的文字提示。
- 应用图标以米白手绘日记页为主体，加入左侧装订、两至三行待办和右上青柠切片；对勾缩小为一行列表元素，不再占据主体。提供 adaptive foreground/background、普通/圆形 mipmap 与 Android 13 monochrome，常见蒙版和 48px 小尺寸下都能辨认日记页而非单独勾号。
- 待办标题区域使用小青柠切片、两三条射线和短波浪线作静态装饰，不增加额外纵向区块，不使用持续动画干扰记录。
- 视觉基准为克制的儿童涂鸦：深墨绿略不均匀线条、纸白/浅薄荷底色和少量柠檬黄；不使用纸张纹理，不旋转正文文字。只有模型服务厂商标识允许基于官方 Logo 核心轮廓进行夸张手绘，其他图标保持项目原创。
- 应用自有按钮、卡片、开关、文本框、选择芯片、分段控件、进度条、空状态、弹窗和 Snackbar 均使用统一 Doodle 组件包装；系统状态栏、键盘、权限弹窗和系统文件选择器保持系统样式。
- 分组可选项目自绘小图标与低饱和颜色；状态必须同时提供文字或图形，不仅依赖颜色。
- 动效遵循系统动画缩放；系统关闭动画时应用不得强制播放，无需增加重复的应用内“减少动效”开关。

## 6. 数据模型

所有业务实体都包含同步元数据：

### TodoItem

| 字段 | 类型 | 说明 |
|---|---|---|
| id | String | UUID 主键 |
| date | String | `yyyy-MM-dd` |
| title | String | 1 至 80 字符 |
| note | String | 0 至 300 字符 |
| isCompleted | Boolean | 完成状态 |
| priority | Int | `0` 低、`1` 普通、`2` 高，默认普通 |
| groupId | String | 一级分组 UUID，旧数据指向固定 ID 的内置“日常” |
| dueDate | String? | 可选截止日期 `yyyy-MM-dd` |
| dueTime | String? | 可选本地截止时间 `HH:mm`，没有时间时保持 null |
| dueAt | Int64? | 有具体时间时的 UTC Unix 毫秒 |
| dueZoneId | String? | 解析具体时间使用的 IANA 时区 ID |
| reminderAt | Int64? | 独立单项提醒 UTC Unix 毫秒 |
| recurrence | String | `none`、`daily`、`weekdays`、`weekly`、`monthly` 或版本化自定义规则 |
| recurrenceSourceId | String? | 重复链的稳定来源 UUID |
| sortOrder | String | 稳定排序值 |
| createdAt | Int64 | UTC Unix 毫秒 |
| updatedAt | Int64 | UTC Unix 毫秒 |
| deletedAt | Int64? | 软删除时间 |
| deviceId | String | 最后修改设备 UUID |
| revision | Int64 | 单调递增逻辑版本 |

### TodoGroup

包含 UUID、名称、版本化自绘 `iconKey`、低饱和颜色键、排序值、是否内置默认分组及同步元数据。内部兼容字段 `isInbox` 与固定 ID 保持不变，但用户可见名称为“日常”。分组软删除后，其有效待办回退显示在“日常”；永久删除待办不会删除分组。2.7.2 仅规范化旧占位图标键，不改变 schema 或 Todo 引用。

### TodoStep

包含 UUID、todoId、标题、完成状态、排序值及同步元数据。步骤跟随主待办参与导入导出和 WebDAV v2；主待办永久删除时步骤正文一并清除。

### TodoTombstone

只包含 todoId 主键、deletedAt、updatedAt、deviceId 与 revision。它不进入任何可见列表，不包含标题、备注、时间或分组信息，仅用于阻止旧快照恢复已永久删除的 Todo。

### DailyReview

在 TodoItem 的同步元数据基础上继续包含 `date`、`highlight`、`challenge`、`learning`、`tomorrowFocus` 和 `mood`。2.7 界面只编辑 `challenge` 与合并后的 `highlight` 内容。有效记录按日期唯一。

### DailySummary

在同步元数据基础上包含 `date`、`content`、`provider`、`model` 和 `generatedAt`。有效记录按日期唯一。

### RangeSummary

| 字段 | 类型 | 说明 |
|---|---|---|
| id | String | UUID 主键，每次生成创建新记录 |
| rangeStart | String | 起始日期 `yyyy-MM-dd` |
| rangeEnd | String | 结束日期 `yyyy-MM-dd` |
| periodType | String | `week`、`month`、`quarter` 或 `custom` |
| prompt | String | 本次一次性总结指令 |
| content | String | 最终总结正文 |
| providerId | String | 本机供应商稳定 ID，仅作来源说明 |
| providerName | String | 生成时供应商显示名 |
| model | String | 生成时实际模型名 |
| includeExistingSummaries | Boolean | 是否读取已有每日总结 |
| generatedAt | Int64 | 生成时间 |
| updatedAt/deletedAt/deviceId/revision | 同步元数据 | 支持历史软删除与跨设备合并 |

### AppMetadata

保存安装实例 `deviceId`、业务 schema 版本、旧版迁移版本和最近同步信息，不参与远端业务快照。

## 7. 技术方案

- Kotlin 2.x、Jetpack Compose、Material 3。
- 单 Activity + Navigation Compose；MVVM 与 `StateFlow` 单向数据流。
- Room/SQLite 管理业务数据，所有合并写入在事务内完成。
- OkHttp 统一处理 LLM 与 WebDAV 请求。
- WorkManager 执行约束网络可用的后台同步。
- Android Keystore AES/GCM 保存 LLM 与 WebDAV 敏感配置。
- `LocalDate` 用于 UI 日期，持久层使用 ISO 日期字符串。
- Room schema 为 7。
- WebDAV JSON 使用格式版本 2；新客户端可读取 v1 并迁移缺失字段，但只写 v2。v2 顶层新增 `groups`、`steps` 与 `todoTombstones`，Todo 增加计划/截止/提醒/重复/分组字段。
- `compileSdk/targetSdk 36`，最低 API 26，交付只验证 64 位 ARM 设备。
- release 构建仅输出 `arm64-v8a` APK；正式商店发布前必须替换长期发布签名。

## 8. 视觉与交互规范

- 关键词：清新、专注、克制、儿童涂鸦、手绘秩序感。
- 页面背景使用纸白或低饱和中性色，深墨绿用于线条，薄荷绿用于品牌和关键动作，柠檬黄只作小面积强调。
- 常规容器圆角不超过 16dp，避免所有区域都做成悬浮大卡片。
- 待办页优先保证扫描效率，复盘页优先保证长文本输入舒适。
- 应用内全部可见功能图标使用 Compose Canvas 或项目 VectorDrawable 自绘，不使用 Emoji、Unicode 功能箭头、Material Icons 或 Android 平台 drawable；模型厂商图标按官方 Logo 核心轮廓进行夸张手绘，不直接嵌入官方或第三方资产。
- 应用自有 Switch、按钮、输入框、芯片、分段控件、卡片、进度条、弹窗和 Snackbar 使用统一涂鸦组件与形状 token，同时保留 Material 语义、键盘行为和至少 48dp 触控目标。
- 启动图标使用 Android Adaptive Icon，品牌主体位于系统安全区，同时适配圆形、圆角方形及 Android 13 主题图标遮罩。
- 支持浅色、深色、边到边布局和系统字体缩放；应用内返回采用提交后执行，且 `MainActivity` 退出系统预测返回动画，拖动期间完全没有页面预览。
- 触控目标不小于 48dp，状态不只通过颜色表达。
- 动画以 150 至 250 毫秒为主，覆盖待办新增/删除/撤销、完成状态、进度、左滑回弹和页面切换，不得阻塞输入、造成布局跳动或持续循环播放，并遵循系统动画缩放设置。

## 9. 验收标准

1. ARM64 Android 设备或模拟器可安装、启动和完成冷启动。
2. 新增三条待办、完成一条后显示 `1 / 3` 和 `33%`。
3. 编辑、删除、取消完成即时反映到列表与进度。
4. 不同日期的数据互不串联。
5. 每日待办页不直接显示复盘字段或 LLM 总结。
6. 点击复盘入口进入独立页面，输入内容返回并重启后仍存在。
7. LLM 配置只在设置二级页；每日生成位于复盘，范围生成位于总结一级页，并共同受智能总结总开关约束。
8. WebDAV 首次同步可创建远端文件；第二设备可下载并合并同一份快照。
9. 冲突、软删除和重复同步不会生成重复记录或恢复旧内容。
10. Room 1/2 与 Drift 3 测试数据库升级后记录数量和关键字段一致。
11. 单元测试、Lint/编译检查通过，APK 中不包含明文凭据。
12. GitHub 保留 Flutter 分支，`main` 为原生实现，并提供可下载的 ARM64 APK。
13. 主页和复盘页的待办列表均不显示常驻编辑、删除图标；点击正文可编辑，左滑仅露出删除按钮且不会自动删除。
14. 复盘页的待办状态变化立即反映到进度，并用于后续智能总结。
15. 外观模式重启后保持；两类提醒可独立启用、修改时间和关闭。
16. JSON 备份可导出并重新导入；损坏文件不会改变本地数据，凭据不会进入备份。
17. 启动图标在常见自适应遮罩和主题图标模式下主体完整、无异常裁切。
18. 已删除待办进入回收站，可查看原日期并恢复；同步后旧设备不会复活删除记录。
19. 设置主页不直接显示 WebDAV 输入框，WebDAV 配置和操作全部位于独立二级页。
20. 复盘页仅显示“解决了什么问题？”和“随便写写”两个文字输入区域，不显示心情评分。
21. 打开旧复盘时四个旧字段内容无损显示；首次编辑“随便写写”后合并内容不会丢失。
22. 待办左滑操作区可通过右滑、点击正文或点击其他待办收回，同一时间最多展开一条。
23. 待办可设置优先级、移动日期和复制；移动与复制后的数据、排序和同步元数据符合 FR-02。
24. 删除后可在 6 秒内撤销；新增、删除、撤销、完成、进度和页面切换具有克制且不阻塞操作的动效。
25. Room schema 4→5、5→6、6→7 迁移均非破坏；WebDAV v1 可读取迁移，v2 字段可完整解析和合并。
26. 两个复盘输入区分别默认显示约 4 行和 8 行，智能总结主按钮为右对齐紧凑按钮且触控目标合格。
27. 旧版单供应商 LLM 配置升级后成为默认供应商，API Key 仍只存在于 Keystore 加密数据中。
28. 可保存至少三个供应商，切换默认供应商后每日总结使用新配置；单次临时覆盖不改变默认项。
29. 模型刷新按协议解析模型列表，失败后仍可手填保存，错误和日志均不包含 API Key。
30. 设置页模型服务入口进入独立卡片列表，支持添加、编辑、复制、删除、启用、排序和连接测试。
31. 每日复盘可选择快捷或自由指令生成总结；最近指令最多 10 条并支持收藏。
32. 总结底部导航页可生成周、月、季度和不超过 93 天的自定义范围总结，并保留多次历史。
33. 超长季度输入按日期分段后综合；取消或任一请求失败不写入不完整历史。
34. RangeSummary 经 Room 5→6→7 迁移、WebDAV v1→v2 和 JSON 导入导出后内容与同步元数据保持一致。
35. WebDAV、导出文件和 Room 中不包含供应商 API Key、Base URL、模型缓存、收藏或最近指令。
36. 侧边返回只拖动后取消时路由、页面状态和复盘草稿保持不变；松手完成后只弹栈一次，一级总结/设置先回待办。
37. 外层主导航与页面顶部栏不重复消费状态栏；所有一级、二级页面标题均只保留一份安全区，待办标题目标间距约 10dp。
38. 新安装智能总结默认关闭；关闭时复盘无生成入口、总结页只读历史且任何生成方法都会拒绝请求；已有模型服务升级用户保持开启。
39. 旧待办经 6→7 迁移后进入固定 ID 的内置“日常”且标题、日期、备注、完成、优先级与同步元数据不变；2.7.0 的“收件箱”仅改名一次，重复启动不重复增加 revision。
40. 待办可分别设置计划日、截止日/时间与提醒；日期型截止不产生伪造的 23:59，逾期事项在当前日可见。
41. 一级分组可创建、重命名、排序、折叠和软删除；每条待办仅属于一个分组，不支持嵌套。
42. 待办步骤可新增、编辑、排序、勾选和删除，列表显示完成数；标题、备注和步骤均可被本地搜索命中。
43. 每天、工作日、每周、每月与自定义重复事项完成后生成下一实例，旧实例保持完成且不会重复生成。
44. 单项提醒在新增/编辑后调度，在完成、软删除和永久删除后取消；权限拒绝或系统延迟不影响待办本地操作。
45. WebDAV v1 可迁移读取并创建 v2；v2 的分组、步骤、提醒、重复与墓碑在双设备合并及 JSON 导入导出后保持一致。
46. 回收站可多选恢复或永久删除并可清空；永久删除移除正文但保留最小墓碑，旧设备同步后不能复活事项。
47. 应用可见图标与自有基础控件统一为克制儿童涂鸦风，源码不直接渲染 Material Icons，不使用纸张纹理或持续装饰动画。
48. `MainActivity` 明确设置 `android:enableOnBackInvokedCallback="false"`；侧边返回拖动到任意距离都不改变页面视觉，取消保持状态，提交只执行一次返回。
49. 模型服务页在已有服务时仍能看到页头“新增”和列表底部“新增模型服务”；点击先选预设，新建后旧服务不被覆盖，首次保存前可手动获取模型。
50. 待办更多菜单的详情、移动日期、复制、删除和优先级使用语义不同的项目自绘图标，源码不回退到 Android/Material 默认图标。
51. 当天且无搜索时显示高度不超过 72dp 的紧凑进度；逾期、计划中和搜索时不显示所选日进度。
52. 搜索默认折叠为 48dp 图标，与 48dp 分组按钮同排；展开后支持标题、备注和步骤，关闭清空并恢复原视图，旋转后查询与展开状态保持。
53. 内置固定分组的新安装名称为“日常”；2.7.0 升级改名幂等，不创建重复分组、不改变关联 Todo，Room 保持 7、WebDAV 保持 v2。
54. 首次点击搜索时输入框稳定展开并获得焦点，初始未聚焦回调不会造成闪烁关闭；只有获得过焦点后失焦且查询为空才自动折叠。
55. 快速新增的分组图标位于加号左侧，只显示图形但 TalkBack 读出当前名称；选择后新增 Todo 写入正确 `groupId`，连续新增保留选择，进程重启或分组失效回到“日常”。
56. 分组管理提供至少 16 个项目自绘图标；新分组优先使用未占用图标，旧通用占位图标只规范化一次且不改变 Todo 归属。
57. 每个内置模型供应商预设都有基于当前官方 Logo 核心轮廓重绘的独立夸张手绘图标；自定义和未知预设安全使用原创 fallback。
58. 启动图标在普通、圆形、adaptive 和 monochrome 模式下均以手绘日记页、待办行和青柠为主体，小尺寸不再退化成单独大对勾。
59. 应用源码静态扫描不包含 Material Icons、`android.R.drawable`、Emoji 或 Unicode 功能箭头；所有应用自有图标逐页通过浅色、深色、字体放大和 TalkBack 审计。
60. 2.7.2 保持 Room schema 7 与 WebDAV v2，不删除或改写已有待办、凭据和模型服务配置。
61. 2.7.2 发布版本为 `2.7.2` / `2702`，标签为 `v2.7.2`，公开 Release 的 ARM64 APK 回下载后与本地大小和 SHA-256 完全一致。

## 10. 后续方向

- 在 WebDAV JSON v2 基础上加入 OneDrive。
- 将领域模型、冲突合并器和同步格式提取为可复用 Kotlin 模块。
- 评估 Compose Multiplatform 或平台原生桌面 UI；不要求 Android UI 为未来平台妥协。
